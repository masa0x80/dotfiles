#!/bin/zsh

output_delimiter=${1:-/}
project_name=$(pwd | rev | cut -d/ -f 1 | rev)
if [ "$(git rev-parse --is-inside-work-tree 2>/dev/null)" = 'true' ]; then
  project_name=$(git rev-parse --show-toplevel | rev | cut -d/ -f1,2 | tr '/' $output_delimiter | rev)
fi
echo $project_name
