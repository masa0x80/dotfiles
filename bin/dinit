#!/bin/bash

echo -n 'export PATH=' > .envrc
if [ -d bin ]; then
  echo -n '$PWD/bin:' >> .envrc
fi
if [ -d vendor/bin ]; then
  echo -n '$PWD/vendor/bin:' >> .envrc
fi
if [ -d node_modules/.bin ]; then
  echo -n '$PWD/node_modules/.bin:' >> .envrc
fi
echo '$PATH' >> .envrc
direnv allow
