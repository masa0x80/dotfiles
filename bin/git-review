#!/usr/bin/env zsh

() {
  local issue_id=${1:-$(git issue-id)}
  if test -n "$issue_id"; then
    git log --graph --grep $issue_id \
        --format="%C(auto)%h%d %s %C(blue)%cr %C(green)%cN" | \
        fzf --tac --no-sort --tiebreak=index +m \
        --preview 'echo {} | rg -o \[a-f0-9\]\{7\} | head -1 | xargs -I % sh -c "git show %"' \
        --bind "ctrl-y:execute-silent: (echo {} | rg -o \[a-f0-9\]\{7\} | head -1 | tr -d '\n' | pbcopy)" \
        --bind "ctrl-m:execute: (rg -o \[a-f0-9\]\{7\} | head -1 | xargs -I % sh -c 'git show --color=always %') << 'FZF-EOF'
            {}
FZF-EOF"
  else
    commit_hash=$(git log --format="%C(auto)%h%d %s %C(blue)%cr %C(green)%cN" | fzf --no-sort --tiebreak=index +m --preview 'git show {1}' --bind "ctrl-y:execute-silent: (printf {1} | pbcopy)" | rg -o \[a-f0-9\]\{7\})
    test -z "$commit_hash" && return

    num=$(git log --pretty="%h" | rg -n "$commit_hash" | cut -d : -f 1)
    git log --reverse -$num \
        --format="%C(auto)%h%d %s %C(blue)%cr %C(green)%cN" | \
        fzf --no-sort --tiebreak=index +m \
        --preview 'git show {1}' \
        --bind "ctrl-y:execute-silent: (printf {1} | pbcopy)" \
        --bind "ctrl-m:execute: (xargs -I % sh -c 'git show --color=always %') << 'FZF-EOF'
            {1}
FZF-EOF"
  fi
} "$1"
