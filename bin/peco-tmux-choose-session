#!/bin/sh

local peco
peco=$HOME/.zplug/bin/peco
local num
num==$(tmux list-sessions | $peco --initial-index $(tmux list-sessions | awk '/attached.$/ {print NR-1}') | cut -d':' -f 1)
tmux switch-client -t $num
