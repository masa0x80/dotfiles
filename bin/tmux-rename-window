#!/bin/sh

tmux_rename_window() {
  local window_name=$(tmux display-message -p '#W' | cut -d @ -f 1)
  local host_name=$(echo $1 | cut -d . -f 1)
  tmux rename-window "${window_name}@${host_name}"
}
tmux_rename_window $1
