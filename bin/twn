#!/usr/bin/env fish

# Usage:
#
# $ tmux set-window-option -g automatic-rename on
# $ tmux set-window-option -g automatic-rename-format '#(twn #{pane_current_path})'

cd $argv
echo (current_dir)(__fish_git_prompt || echo '')
