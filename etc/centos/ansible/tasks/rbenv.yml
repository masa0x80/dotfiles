- name: prepare to install rbenv
  yum: name={{ item }} state=latest
  with_items:
    - gcc-c++
    - openssl-devel
    - readline-devel
    - libffi-devel
    - libxslt-devel
    - gecode-devel
  sudo: yes
- name: install rbenv
  shell: "anyenv install rbenv"
- name: fix ruby version
  shell: "rbenv install {{ ruby_version }} && rbenv global {{ ruby_version }}"
- name: bundler
  shell: "rbenv rehash && rbenv exec gem install bundler"
